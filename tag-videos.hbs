{{!< default}}

{{#contentFor "extraHead"}}
  <style>
    nav[class="md-tabs"] {
      border-bottom: none;
    }
  </style>
{{/contentFor}}

{{#contentFor "beforeContent"}}
  {{^is "paged"}}
    <section class="mdx-container">
      <section class="mdx-parallax__group mdx-video-spotlight">
        <div class="md-content md-grid" data-md-component="content">
          <div class="md-content__inner">
            <div class="mdx-spotlight">
              {{#foreach posts limit="1"}}
                {{> "components/video-spotlight-figure" action="Watch the latest →"}}
              {{/foreach}}
            </div>
          </div>
        </div>
      </section>
    </section>
  {{/is}}
{{/contentFor}}

<div class="md-main__inner md-grid">
  {{> "components/nav-sidebar"}}

  <div class="md-sidebar md-sidebar--secondary" data-md-component="sidebar" data-md-type="toc">
    <div class="md-sidebar__scrollwrap">
      <div class="md-sidebar__inner">
        <nav class="md-nav md-nav--secondary">
          <label class="md-nav__title" for="__toc">
            <span class="md-nav__icon md-icon"></span>
            Table of contents
          </label>
          <ul class="md-nav__list" data-md-component="toc">
            {{#foreach posts}}
            <li class="md-nav__item">
              <a href="{{url}}" class="md-nav__link">
                <span class="md-ellipsis">
                  {{title}}
                </span>
              </a>
            </li>
            {{/foreach}}
          </ul>
        </nav>
      </div>
    </div>
  </div>

  <div class="md-content" data-md-component="content">
    <div class="md-content__inner">
      {{#tag}}
      <header class="md-typeset">
        <h1 class="gh-pagehead-title">{{name}} from Privacy Guides</h1>
        <p>{{description}}</p>
      </header>
      {{/tag}}

      {{^is "paged"}}
        {{#foreach posts from="2"}}
          {{> "components/post" action="Watch now →"}}
        {{/foreach}}
      {{else}}
        {{#foreach posts}}
          {{> "components/post" action="Watch now →"}}
        {{/foreach}}
      {{/is}}

      {{pagination}}

    </div>
  </div>
  
</div>
